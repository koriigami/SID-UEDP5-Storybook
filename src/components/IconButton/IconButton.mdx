import { Meta, Canvas, Controls } from "@storybook/blocks";
import * as IconButtonStories from "./IconButton.stories";

<Meta of={IconButtonStories} />

# Icon Button

Mirror of Figma node `3863:32746`. Same size ladder as Button, but the
label is invisible and `ariaLabel` is required at compile time.

<Canvas of={IconButtonStories.Primary} />
<Controls />

## Every combination

<Canvas of={IconButtonStories.AllStates} />
